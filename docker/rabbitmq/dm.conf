# Per-object metrics. The default endpoint aggregates across every queue, and an
# aggregate cannot say which queue is backing up or which dead-letter queue has
# stopped being empty - the two states the alerts exist for. The cardinality
# warning in the upstream documentation is about thousands of queues; this broker
# carries five.
prometheus.return_per_object_metrics = true
