# Правки в Документация_по_разработке_DM3.docx

Поштучный перечень к аппруву. Формат документа не трогаем: шрифты, стили
заголовков, таблицы и отступы остаются как есть, правки идут тем же скелетом
"Описание / Состав / Поведение", что и соседние страницы.

Замеры по файлу на 2026-08-18: 4289 абзацев, 202 тысячи знаков, 49 таблиц,
67 картинок исходно (123 после вставки скриншотов), 677 заголовков. В разделе
4.2.3 описано 65 страниц, на сайте 77 пользовательских (44 гостевых, 13 под
входом, 20 модераторских).

По каждому пункту проведен разбор вариантов с adversarial-проверкой обеих
сторон (воркфлоу из 9 агентов, 2026-08-18): аналитик предлагал варианты и
рекомендацию, опровергатель ломал ее. Строка "Рекомендация" — итог после
опровержения. Порядок выполнения: сначала решение по D2 (адреса), затем A,
затем B и C — чтобы новые разделы рождались сразу с адресами, а перестановки
не делались дважды.

Статусы: `[ ]` ждет решения | `[x]` одобрено | `[-]` отклонено.

**Исполнено 2026-08-18** по аппруву владельца на все рекомендации разом:
A1-A3, B1-B7, C1-C9 и D2 внесены в docx одним прогоном
`scripts/docx/` + сборочный скрипт сессии. Тексты новых разделов написаны
агентами по коду реальных страниц (5 пакетов, 27 записей), не из головы.
Итог: 3053 -> 3529 абзацев, 49 -> 50 таблиц (действия над игрой и блогом),
+21 скриншот, документ 21.1 МБ. Проверено: части пакета кроме document.xml
и rels канонически идентичны, буквы "ё" и елочек в тексте нет. Оглавление
Word обновит при открытии (F9 или пересчет полей). D1 отклонен владельцем
вместе с рекомендацией.

---

## A. Не на своем месте

Раздел 4.2.3 называется "Страницы сайта", и в нем лежит семь записей, которые
страницами не являются. Они занимают номера в сквозной нумерации страниц,
из-за чего счет страниц по документу не сходится ни с чем.

- `[x]` **A1.** 4.2.3.1.1 "Авторизация", 4.2.3.1.2 "Регистрация",
  4.2.3.1.3 "Восстановление доступа" — модальные окна, они сами так и подписаны.
  **Рекомендация:** переименовать 4.2.4 "Немодальные диалоговые окна" в
  "Диалоговые окна" и дописать модалки в его хвост как 4.2.4.4-4.2.4.6;
  оставшиеся страницы 4.2.3.1.4-6 перенумеровать в .1-.3. Проверено грепом:
  на номера модалок не ссылается никто, из зоны перенумерации живет одна
  ссылка "см. 4.2.3.1.5" — править ровно ее; альтернатива с отдельным разделом
  модальных окон сдвигает 4.2.4-4.2.5 и ломает ~34 ссылки. Сопутствующее,
  вскрытое опровергателем: переписать вводный абзац 4.2.4 (он определяет
  раздел через немодальность), написать модалкам местные секции
  "Назначение / Триггер" заново, и синхронно поправить шесть ключей MAPPING в
  скрипте вставки скриншотов.
- `[x]` **A2.** 4.2.3.5.13-16 (отправка игры на премодерацию, снятие, удаление,
  сброс даты набора) — не страницы: по коду это один общий диалог подтверждения,
  вызываемый из панели игры, у записей нет ни адресов, ни скриншотов, ни
  внешних ссылок. **Рекомендация:** одна запись "Действия над игрой" в
  переименованном 4.2.4 с таблицей: действие, предусловие по статусу, кому
  доступно, эффект (переходы статусов, мягкость удаления, сброс даты) — колонка
  эффектов обязательна, без нее спека перестает утверждать, что происходит по
  нажатию. При переносе сверить содержимое с кодом: docx уже разошелся с ним
  (удаление — в docx Moderator+ и мастер при <10 постов с "можно отменить";
  в коде кнопка в панели у SeniorModerator+ с "действие необратимо", а право на
  сервере есть и у самого мастера без ограничения по постам —
  GameIntentionResolver.cs:66; в docx "Снять с
  премодерации", в коде "Выпустить"). Если A1 отклонен — та же таблица
  подразделом внутри 4.2.3.5.
- `[x]` **A3** (новое, найдено опровергателем A2)**.** Действия над блогом —
  премодерация и удаление из панели блога, зеркало игровых — в docx
  отсутствуют вовсе. **Рекомендация:** описать в той же записи 4.2.4, что и
  A2, второй таблицей или общей с колонкой "модуль", чтобы зеркальная пара
  зон не осталась полуохваченной.

## B. Страниц нет в документе, а на сайте они есть

Проверял поиском по всему тексту, не только по заголовкам. Ноль означает, что
слова нет в документе вообще.

- `[x]` **B1.** **Пульс** (`/pulse`), упоминаний 0. **Рекомендация:** полный
  скелет отдельной записью в 4.2.3.4 "Сообщество", рядом с Глобальным чатом.
  Ссылка стоит в шапке, тестировщик альфы откроет ее в первые минуты, а
  поведение ленты, фильтра и сортировок из UI не выводится. Состав ленты не
  пересказывать — ссылкой на 4.2.2.14.
- `[x]` **B2.** **Подписки** (`/subscriptions`), упоминаний 0.
  **Рекомендация:** полный скелет, компактный.
- `[x]` **B3.** **Личный блокнот** (`/notepad`), упоминаний 0.
  **Рекомендация:** полный скелет, написанный как дифф от уже описанной
  идиомы заметок игры и блога (35 упоминаний): что общее — ссылкой, свое —
  явно.
- `[x]` **B4.** **Рецензии на игры**, упоминаний 0. **Рекомендация:** полный
  комплект — контентный блок "Рецензия на игру" в 4.2.2 (не путать с
  "Оценкой поста" 4.2.2.14: BBCode, любой сентимент, одна рецензия на пару
  автор-игра, право писать решает сервер по участию, без лайков) плюс три
  записи страниц: вкладка игры и парные страницы профиля. Блок
  самодостаточен, ссылка на рекомендации — только за идиому карточки;
  порядок с B5 не важен.
- `[x]` **B5.** **Рекомендации на профиле** — одно упоминание в глоссарии 6.2,
  страниц нет. **Рекомендация:** контентный блок "Рекомендация" в 4.2.2 плюс
  две записи страниц профиля (полученные/написанные). Определение из 6.2
  разворачивается в блоке, не переизобретается. Независим от B4.
- `[x]` **B6.** **Пользовательское соглашение** и **Политика
  конфиденциальности** — страниц нет. **Рекомендация:** две короткие записи
  полным скелетом в 4.2.3.2; содержимое самих текстов не пересказывать.
- `[x]` **B7.** **Награды и достижения** — на сайте вкладка профиля, бейджи и
  три страницы модерации; в docx почти ничего. **Рекомендация:** контентные
  блоки в 4.2.2 (награда, серия, достижение с цепочками по метрикам) плюс
  расширение записи "Профиль" вкладкой достижений. Модель системы наград
  вводится ровно один раз — здесь; админские записи C7 и C8 ссылаются сюда,
  а не наоборот.

## C. Модерация: описано 9 страниц из 20

Стратегия группы (устояла у опровергателя): все 11 полным скелетом — это
единственный вариант, сохраняющий единообразие скелета и механику вставки
скриншотов; тривиальным страницам — короткий скелет в 3-6 предложений;
понятия уровня продукта — в глоссарий раздела 6, страница держит только UI
и гейт.

- `[x]` **C1.** Модераторы. Короткий скелет; состав — таблица зон
  ответственности (закрепленные разделы форума, курируемые игры и блоги,
  порядок от старшей роли): кураторство и есть то, чего из названия не видно.
- `[x]` **C2.** Премодерируемые блоги. Своя запись с опорой на глоссарий 6.4
  плюс парная правка: из описания 4.2.3.8.2 убрать "и блогов", иначе две
  записи противоречат друг другу.
- `[x]` **C3.** Баны. Полный скелет со ссылками на существующие 6.3 (семантика
  двух видов бана уже там, не дублировать) и 4.2.2.22. В поведении: просмотр
  Moderator, досрочное снятие SeniorModerator+, создание бана происходит не
  здесь, а из профиля через диалог 4.2.4.2.
- `[x]` **C4.** Карточка обращения. Состав — композиция готовых блоков
  "{Обращение} (см. 4.2.2.23)" и "{Ответ} (см. 4.2.2.24)", модель Ticket не
  пересказывать. В поведении только свое: маршрут, вход из списков 4.2.3.8.7-8,
  гейты (поддержка — Admin), отличие от гостевого отслеживания по токену.
- `[x]` **C5.** Запросы на смену имени. Короткий скелет, явно связать обе
  стороны процесса, и согласовать термины с 4.2.3.3.4: там "смена логина"
  требует "одобрения администрации", фактический гейт — SeniorModerator.
- `[x]` **C6.** Теги игр. Скелет плюс глоссарная статья, именованная
  "Теги игр" — в документе полсотни употреблений слова "тег" про BBCode,
  их нельзя сталкивать. Плюс по строке в существующие 4.2.3.5.1 (строка
  тегов-ссылок) и 4.2.3.5.10 (селектор тегов в форме), плюс фильтр в записи
  каталога игр.
- `[x]` **C7.** Награды, серия, типы — три отдельные записи, короткие и
  равноправные; модель системы наград не вводить, ссылаться на B7.
- `[x]` **C8.** Достижения. Скелет про сам конструктор (UI, гейт
  SeniorModerator); понятийная модель (категории, тиры, метрики, цепочки) —
  там же, где для C7, без перечня конкретных метрик.
- `[x]` **C9.** Сбор средств. Короткий скелет, взаимные ссылки с гостевой
  записью 4.2.3.2.7 "Помочь проекту" (обе страницы зоны — гейт Admin).

## D. Организация раздела страниц

- `[-]` **D1.** ~~Разложить 4.2.3 по ярусам доступа~~ — **рекомендую
  отклонить как ошибку каталога**, и опровергатель это подтвердил с двух
  сторон. Ярусы — порядок внутреннего тестирования этапа 1; альфа и бета
  (этапы 2 и 4 плана перехода) тестируют сайт целиком на копии базы DM2,
  спека переживет все этапы. Перекладка ломает сквозную нумерацию, живые
  ссылки "см. 4.2.3.x" (13 штук) и привязку 56 вставленных скриншотов, а
  ярус по гейту маршрута дает абсурд: "Создание игры" оказалось бы в
  гостевом разделе. Замена: метка доступа в адресной строке из D2 — только
  там, где она информативна ("вход", "модерация", "гость, содержимое по
  входу" для шести псевдооткрытых); гостевым страницам метка не ставится,
  45 одинаковых "гость" — шум.
- `[x]` **D2.** Адрес маршрута у каждой записи страницы. **Рекомендация:**
  делать, единым форматом, строкой после заголовка и скриншота (порядок
  "заголовок — скриншот — адрес" фиксируется, скрипт вставки скриншотов
  правится под него). В шапке 4.2.3 — одна фраза с примером, как читать
  шаблоны вида `:id`. Решение принять до реализации B и C: 13 новых разделов
  должны родиться сразу с адресами. Это же предусловие любой автоматической
  сверки docx с роутером — без адресов сверять нечего.
- `[x]` **D3.** Скриншоты страниц — СДЕЛАНО 2026-08-18. Снято 89 страниц
  безголовым Chromium через Playwright: гость и сид-аккаунт SolohinLex (данные
  сидовые, лежат в открытую в `e2e/fixtures/auth.ts`, поэтому вход не потребовал
  ничьих паролей). В docx вставлено 56 — по одному после заголовка каждого
  описанного раздела страницы, включая три модальных окна входа. Проверено:
  абзацев ровно +56, таблицы 49 -> 49, все 12 встроенных шрифтов на месте,
  стили/нумерация/колонтитулы канонически идентичны прежним. Остальные 33
  снимка — страницы, которых в документе пока нет (пункты B и C): вставятся,
  когда появятся их разделы. Пересъемка: `scripts/docx/capture.cjs` +
  `scripts/docx/insert_shots.py` (перенесены в репозиторий из сессионного
  scratchpad); съемка идет с паузами под глобальный лимитер API.

## E. Проверено, правок не требует

- Лайки описаны в 4.2.2.4 (сообщение), 4.2.2.16 (публикация), 4.2.2.17 (тема),
  4.2.2.18 (комментарий). Все четыре живы: в `LikeEntityType` остались Comment,
  Topic, Message, Publication, Post. Удалили мы лайки оценок постов, а их
  документ никогда и не описывал.
- Скелет "Описание / Состав / Поведение" единообразен по всем 65 страницам.
- Наставничество (29 упоминаний), премодерация (68), уведомления (38),
  заметки (35), жалобы (19) покрыты полно.
- Глоссарий 6.3 уже содержит развернутую семантику обоих видов бана, а блок
  4.2.2.23 — полную модель обращения: новые записи C3 и C4 ссылаются на них,
  а не дублируют.
