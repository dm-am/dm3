<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
    </PropertyGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="DM.Infrastructure.Persistence" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\DM.Domain.Core\DM.Domain.Core.csproj" />
    </ItemGroup>

    <ItemGroup>
        <!-- Autofac only for the module that declares this assembly's lifetimes. No
             options packages: this assembly reads no configuration and ships no
             configuration extension, which is what the account domain has them for. -->
        <PackageReference Include="Autofac" />
        <PackageReference Include="FluentValidation" />
    </ItemGroup>
</Project>
