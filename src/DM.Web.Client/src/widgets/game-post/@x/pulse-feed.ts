/**
 * Cross-import surface for widgets/pulse-feed (FSD @x).
 *
 * The pulse feed renders game posts with the same widget the game room uses;
 * a second renderer would drift from it immediately.
 */
export { GamePost } from "..";
