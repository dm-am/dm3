/**
 * Cross-import surface for widgets/rated-posts (FSD @x).
 *
 * Every list of rated posts draws a post with the same widget the game room
 * uses; a second renderer would drift from it immediately.
 */
export { GamePost } from "..";
