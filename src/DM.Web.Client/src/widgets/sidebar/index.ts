// The blocks of the two sidebars are addressed by relative path from inside the
// widget; this barrel carries only what is mounted from outside it — the two
// sidebars themselves and the poll card the community page reuses.
export { default as LeftSidebar } from "./LeftSidebar.vue";
export { default as RightSidebar } from "./RightSidebar.vue";
export { default as PollCard } from "./PollCard.vue";
