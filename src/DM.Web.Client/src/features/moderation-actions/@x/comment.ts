/**
 * Cross-import surface for features/comment (FSD @x).
 *
 * Warning a comment's author is a moderation action, and the dialog that
 * collects it belongs to moderation-actions. This is the single sanctioned door
 * into it from the comment feature.
 */
export { WarningDialog } from "..";
