/**
 * Cross-import surface for features/publication (FSD @x).
 *
 * A publication card renders the same heading row as a topic card and reuses it
 * rather than growing a second copy.
 */
export { TopicCard } from "..";
